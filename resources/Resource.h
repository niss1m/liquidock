#pragma once

// Shared by the resource script and the code that loads from it, so the icon
// is named in one place rather than as a number in two.
#define IDI_APPICON 1

#define LIQUIDOCK_VERSION_COMMAS 0, 1, 0, 0
#define LIQUIDOCK_VERSION_STRING "0.1.0"
